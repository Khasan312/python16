ssh-keygen -это нужно для генерации ssh ключа
.ssh - это скрытая директория ssh настроек
if_rsa.pub - в ней хранится открытый ключ

GIT - система контроля версий
git init  - инициализация
.git - создается скрытая папка

git add <file_name> <.> -для проверки статуса
git commit -m 'comments" - он фиксирует файл и создает хэш сумму
git push origin master - отправка всех локальных коммитов в удаленный репозиторий

git remote add origin git@github.com:Khasan312/python16.git

git clone - просто клон проекта  из удаленного сервера 

git config --global user.name 'Khasan'
git config --global user.email 'hasan.aliev.2000@mail.ru'


